# README 

This repository includes a single SQL file where I created a basic database using the command line. The database includes:
1. 3 basic tables that are populated
2. SQL queries to get specific data from the database
  
